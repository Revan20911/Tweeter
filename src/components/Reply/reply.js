import {useState, useEffect, useRef} from 'react';

const Reply = ({user, tweet}) => {
    currentTweet = useState(tweet)
    currentUser = useState(user);

    const tweetBox = useRef(null);





}

export default Reply;